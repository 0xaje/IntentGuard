# 1 - IntentGuard makes the mismatch impossible to miss.

IntentGuard makes the mismatch impossible to miss by comparing user goals directly against wallet requests. Point to the three-step flow from stating intent to decoding the request and applying policy. The model only normalizes and explains, while deterministic code makes the actual decision. Transition to the three exact decision outcomes.

# 2 - Autonomous finance earns trust by proving what it means.

Autonomous finance earns trust by proving what it means, not by asking users to blindly trust a risk score. IntentGuard is live, open-source, anchored on Base, and fully ready for the Orion ecosystem. Point to the core badges at the bottom to summarize our end-to-end delivery. Thank you, and we invite your questions on our policy rules, EIP-712 permits, registry security, evaluator trust, and smart-account enforcement.

# 3 - IntentGuard

Welcome to IntentGuard. We are building the transaction-intent firewall for autonomous finance, designed for Base and the Orion Builder Hackathon. Point to the Orion Builder Hackathon badge and the core triad of read-only, confirm-first operation. Transition now to the core problem in autonomous interactions.

# 4 - Built for Orion’s agent economy.

Autonomous agents inside Orion need a reliable pre-signing trust layer, and IntentGuard fits that exact gap. We provide a reusable policy and receipt engine across agents, launchpads, wallets, and Base dApps. The business model is simple: integration and API revenue for products that require verifiable autonomous actions without taking custody. Point to the integration surfaces on the screen to show how easily this plugs into existing agent workflows. And remember, we never touch private keys or hold funds. Let's look at why our judging strategy stands out.

# 5 - The wallet shows a request. It does not show intent.

Wallets show you raw requests, but they never show you intent. Point to the gap between human belief and chain effect. The dangerous gap is the distance between what a person thinks they are approving and what the chain actually executes. Transition to how IntentGuard bridges this exact gap.

# 6 - One decision. Three outcomes. Zero false confidence.

One decision yields three outcomes with zero false confidence. Point to the match, mismatch, and cannot verify outcomes. A match means policy checks passed, not guaranteed safety, while incomplete evidence always fails closed. Transition to our unique market wedge.

# 7 - This is intent fidelity—not another risk score.

This is intent fidelity, not another generic risk score. Point to the comparison table contrasting existing security dashboards with our intent-verification wedge. Simulation and reputation remain valuable evidence sources, but the real question is whether the action matches authorization.

# 8 - We win by being the agent that refuses to guess.

We win this hackathon by being the agent that refuses to guess. Our judging case rests on four pillars: absolute usefulness in stopping signing mistakes, complete execution with live demos and verified contracts, true originality in intent-to-effect verification, and rigorous security. Point to the four numbered blocks to highlight our execution proof. Some judges might ask if we catch every custom routing trick, so be clear: if evidence is incomplete, we return cannot verify rather than giving false confidence. Before value moves, prove the action matches intent. Let's wrap up with our final thoughts.

# 9 - The agent investigates. Deterministic code decides.

We separate human language from strict on-chain execution so that AI never holds the keys. The model normalizes your goal and gathers evidence, but only deterministic code issues the verdict. If evidence is missing or ambiguous, the engine defaults to cannot verify rather than guessing. Point to the separation between the agent investigation layer and the immutable policy engine. And notice how this guarantees that even if a model hallucinates, the hard limits remain absolute. Moving forward, let us examine how this architecture enforces a strict trust boundary.

# 10 - Safety is a product feature—and a trust boundary.

We earn trust by refusing powers we do not need, operating entirely read-only and non-custodial. We never request private keys, hold funds, or execute arbitrary transactions, and unknown selectors always fail closed. Point to the explicit trust boundary on screen showing what we never touch. Now, judges often ask if an external signer can bypass us entirely. Our honest answer is yes, because our MVP protects the approval flow it integrates into, while universal enforcement requires wallet-level integration. This focus on verifiable constraints leads directly to how the system performs in a live demonstration.

# 11 - The demo proves the product in under 90 seconds.

We prove the product in under ninety seconds with a live cold-browser demo requiring zero wallet setup. Point to the mismatch scenario where an airdrop claim unexpectedly requests unlimited ERC-20 approval. Next, show the exact match state and how unknown selectors safely trigger a cannot-verify state. If the RPC fails during the run, we simply fall back to cached decoder metadata. Now watch how every single one of these verdicts generates a verifiable audit trail on-chain.

# 12 - Every verdict leaves a verifiable trail.

Every decision leaves a verifiable cryptographic trail anchored on Base Sepolia. Point to the receipt breakdown detailing the intent, request, and evidence hashes bound by an EIP-712 signature. Every receipt includes an explicit expiry and revocation mechanism without taking custody of user funds. And remember, the registry is a proof of compliance, not a safety oracle that guarantees third-party contract security. This brings us right to how this architecture fits into Orion's broader agent economy.
