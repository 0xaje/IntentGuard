# Git Workflow Rule

- Always commit and push changes to the remote repository (`https://github.com/0xaje/IntentGuard` / `origin main`) after completing meaningful changes or tasks.
- Keep commits clear, focused, and well-described.
- Ensure tests pass (`pnpm test`) before pushing.
